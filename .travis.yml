language: python

python:
  - 3.6

before_install:
  - pip install -r requirements.txt

script:
  - nosetests

deploy:
  provider: script
  script: deploy.sh
  skip_cleanup: true
  on:
    branch: master
